@model IEnumerable<Chushka.ViewModels.Models.OrderViewModel>

<div class="mt-3 mb-5">
    <h1 class="text-center">All Orders</h1>
    <hr class="hr-2 bg-dark" />
    <table class="table w-75 mx-auto table-hover">
        <thead>
            <tr class="row">
                <th class="col-md-4">Order ID</th>
                <th class="col-md-2">Customer</th>
                <th class="col-md-2">Product</th>
                <th class="col-md-2">Ordered On</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="row">
                    <td class="col-md-4">@item.OrderId</td>
                    <td class="col-md-2">@item.Customer</td>
                    <td class="col-md-2">@item.Product</td>
                    <td class="col-md-2">@item.OrderedOn</td>
                </tr>
            }
        </tbody>
    </table>
</div>